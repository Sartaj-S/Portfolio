readme.txt

This is a terminal application that accepts command line arguments for text files and allow the user to choose which sorting alogirthm they want the files sorted with. The program shows a menu to the user which also allows them to add file names that they may have missed or bypass the command line input altogether and simply add files via the interface. The sorting options available are bubble sort, insertion sort, and quick sort.
